name: Deploy Smart Energy Grid to Elastic Beanstalk

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  EB_APPLICATION_NAME: smart-energy-grid
  EB_ENVIRONMENT_NAME: smart-energy-grid-env
  S3_BUCKET: smart-energy-grid-deployments
  DEPLOY_PACKAGE: smart-energy-grid-${{ github.sha }}.zip

jobs:
  deploy:
    name: Deploy to Elastic Beanstalk
    runs-on: ubuntu-latest
    
    steps:
      # Checkout code
      - name: Checkout Code
        uses: actions/checkout@v4

      # Configure AWS credentials
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      # Setup Go
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      # Download dependencies
      - name: Download Go Dependencies
        run: go mod download

      # Run tests
      # - name: Run Tests
      #   run: go test ./... -cover

      # Build the application
      - name: Build Application
        run: |
          echo "Building Go application..."
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o bin/api ./cmd/api
          echo "Build complete!"

      # Create deployment package
      - name: Create Deployment Package
        run: |
          echo "Creating deployment package..."
          zip -r ${{ env.DEPLOY_PACKAGE }} \
            bin/ \
            .ebextensions/ \
            .platform/ \
            Procfile \
            Buildfile \
            go.mod \
            go.sum \
            internal/ \
            pkg/ \
            cmd/ \
            -x "*.git*" "*.env*" "*node_modules*" "*.vscode*"
          
          echo "Package created: ${{ env.DEPLOY_PACKAGE }}"
          ls -lh ${{ env.DEPLOY_PACKAGE }}

      # Upload to S3
      - name: Upload to S3
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          BUCKET_NAME="smart-energy-grid-deployments-${ACCOUNT_ID}"
          aws s3 cp smart-energy-grid.zip s3://smart-energy-grid-deployments/smart-energy-grid.zip --region us-east-1
          echo "BUCKET_NAME=${BUCKET_NAME}" >> $GITHUB_ENV

      # Create Elastic Beanstalk application version
      - name: Create EB Application Version
        run: |
         VERSION_LABEL=v1-$(date +%Y%m%d%H%M%S)
          aws elasticbeanstalk create-application-version \
           --application-name smart-energy-grid \
           --version-label $VERSION_LABEL \
           --source-bundle S3Bucket=smart-energy-grid-deployments,S3Key=smart-energy-grid.zip \
           --region us-east-1
           echo "VERSION_LABEL=$VERSION_LABEL" >> $GITHUB_ENV

      # Deploy to Elastic Beanstalk
      - name: Deploy to Elastic Beanstalk
        run: |
          aws elasticbeanstalk update-environment \
            --application-name smart-energy-grid \
            --environment-name smart-energy-grid-env \
            --version-label ${{ env.VERSION_LABEL }} \
            --region us-east-1
          
          echo "Deployment initiated!"

      # Wait for deployment to complete
      - name: Wait for Deployment
        run: |
          echo "Waiting for environment to be ready..."
          
          max_attempts=30
          attempt=0
          
          while [ $attempt -lt $max_attempts ]; do
            status=$(aws elasticbeanstalk describe-environments \
              --application-name ${{ env.EB_APPLICATION_NAME }} \
              --environment-names ${{ env.EB_ENVIRONMENT_NAME }} \
              --region ${{ env.AWS_REGION }} \
              --query 'Environments[0].Status' \
              --output text)
            
            health=$(aws elasticbeanstalk describe-environments \
              --application-name ${{ env.EB_APPLICATION_NAME }} \
              --environment-names ${{ env.EB_ENVIRONMENT_NAME }} \
              --region ${{ env.AWS_REGION }} \
              --query 'Environments[0].Health' \
              --output text)
            
            echo "Status: $status, Health: $health"
            
            if [ "$status" == "Ready" ] && [ "$health" == "Green" ]; then
              echo "✅ Deployment successful!"
              exit 0
            fi
            
            if [ "$status" == "Ready" ] && [ "$health" == "Yellow" ]; then
              echo "⚠️ Deployment complete but health is Yellow"
              exit 0
            fi
            
            if [ "$health" == "Red" ]; then
              echo "❌ Deployment failed - Environment health is Red"
              exit 1
            fi
            
            attempt=$((attempt + 1))
            echo "Waiting... (attempt $attempt/$max_attempts)"
            sleep 20
          done
          
          echo "❌ Deployment timeout"
          exit 1

      # Get environment URL
      - name: Get Environment URL
        if: success()
        run: |
          CNAME=$(aws elasticbeanstalk describe-environments \
            --application-name ${{ env.EB_APPLICATION_NAME }} \
            --environment-names ${{ env.EB_ENVIRONMENT_NAME }} \
            --region ${{ env.AWS_REGION }} \
            --query 'Environments[0].CNAME' \
            --output text)
          
          echo "🚀 Application deployed successfully!"
          echo "📍 URL: http://$CNAME"
          echo "ENVIRONMENT_URL=http://$CNAME" >> $GITHUB_ENV

      # Smoke test
      - name: Smoke Test
        if: success()
        run: |
          echo "Running smoke test..."
          sleep 10
          
          CNAME=$(aws elasticbeanstalk describe-environments \
            --application-name ${{ env.EB_APPLICATION_NAME }} \
            --environment-names ${{ env.EB_ENVIRONMENT_NAME }} \
            --region ${{ env.AWS_REGION }} \
            --query 'Environments[0].CNAME' \
            --output text)
          
          response=$(curl -s -o /dev/null -w "%{http_code}" http://$CNAME/health)
          
          if [ "$response" == "200" ]; then
            echo "✅ Smoke test passed!"
          else
            echo "⚠️ Smoke test returned status code: $response"
          fi